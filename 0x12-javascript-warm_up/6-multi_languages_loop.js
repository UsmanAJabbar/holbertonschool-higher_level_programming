#!/usr/bin/node
// Prints an array with one lie
const lang = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let index = 0; index < lang.length; index++) {
  console.log(lang[index]);
}
