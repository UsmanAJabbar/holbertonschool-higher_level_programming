#!/usr/bin/node
// Adds two numbers
/**
 * add - adds two numbers
 * num1 - first number
 * num2 - second number
 * Return: None
 */
exports.add = function (num1, num2) {
  return parseInt(num1) + parseInt(num2);
};
